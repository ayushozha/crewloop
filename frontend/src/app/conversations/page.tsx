import { DashboardClient } from "../dashboard/DashboardClient";

export const metadata = {
  title: "Conversations · CrewLoop",
};

export default function ConversationsPage() {
  return <DashboardClient />;
}
